# Crawler
Scrapy==2.19.0
itemadapter==0.13.1
requests==2.34.2
pandas==3.0.6
# Training + serving. scikit-learn pinned EXACTLY: the .pkl artifacts in
# server/models/ are a versioned pickle contract (see model_meta.json).
scikit-learn==1.9.1
Flask==3.1.3
flask-cors==6.0.5
gunicorn==26.2.0
python-dotenv==1.2.3
